# ==================== 数据库配置 ====================
# 主数据库连接（请替换为你的实际数据库连接字符串）
PGDATABASE_URL=postgresql://username:password@localhost:5432/database_name

# 开发环境数据库（可选）
PGDATABASE_URL_DEV=postgresql://username:password@localhost:5432/database_dev

# 生产环境数据库
PGDATABASE_URL_PROD=postgresql://username:password@prod-host:5432/database_prod

# ==================== 企业微信配置 ====================
# 企业微信企业ID（从企业微信管理后台获取）
# 路径：我的企业 → 企业信息 → 企业ID
# 格式：ww1234567890abcdef
WEWORK_CORPID=ww1234567890abcdef

# 企业微信应用ID（AgentId）
# 路径：应用管理 → 应用 → 你的应用 → AgentId
# 格式：数字，如 1000001
WEWORK_AGENT_ID=1000001

# 企业微信应用Secret（从企业微信管理后台获取）
# 路径：应用管理 → 应用 → 你的应用 → Secret
# 格式：长字符串
WEWORK_SECRET=abcdefghijklmnopqrstuvwxyz123456

# 企业微信应用Token（用于接收消息回调，可选）
WEWORK_TOKEN=your_token_here

# 企业微信应用EncodingAESKey（用于消息加密，可选）
WEWORK_ENCODING_AES_KEY=your_encoding_aes_key_here

# ==================== 应用配置 ====================
# 应用URL（部署后需要修改为实际域名）
NEXTAUTH_URL=http://localhost:5000

# 应用密钥（用于加密会话token，建议使用随机字符串）
# 可以使用以下命令生成：openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-change-in-production

# ==================== Coze配置 ====================
# Coze工作负载API密钥（已配置）
COZE_WORKLOAD_IDENTITY_API_KEY=your_coze_api_key_here
